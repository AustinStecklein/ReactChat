export default function SecondElement(props) {
    return (
      <div>
        <h1>Using a function instead of a class</h1>
        <p>Must add a hook later... {props.text}</p>
      </div>
    );
  }